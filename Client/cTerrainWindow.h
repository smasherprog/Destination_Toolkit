#ifndef CTERRAIN_WINDOW_H
#define CTERRAIN_WINDOW_H

#include <map>
/*
#include "Gwen/Gwen.h"
#include "Gwen/Controls/WindowControl.h"
#include "Gwen/Controls/Label.h"
#include "Gwen/Controls/TabControl.h"

#include "Gwen/Controls/HorizontalSlider.h"

class cTerrainWindow : public Gwen::Controls::WindowControl{
public:
	GWEN_CONTROL( cTerrainWindow, Gwen::Controls::WindowControl );

	void SetHidden(bool hidden);

	void DontUseTextureWheel( Gwen::Controls::Base* pControl );
	void UseTextureWheel( Gwen::Controls::Base* pControl );
	void SelectTexture( Gwen::Controls::Base* pControl );
	void LandEditing( Gwen::Controls::Base* pControl );
	void InnerRadius( Gwen::Controls::Base* pControl );
	void OuterRadius( Gwen::Controls::Base* pControl );
	void ColorChanged( Gwen::Controls::Base* pControl );

	std::map<void*, uint8_t> TextoIndex;
	cLand& Land;
	Gwen::Controls::TabControl*	m_TabControl;
	Gwen::Controls::HorizontalSlider* InnerSlider, * OuterSlider;
	Gwen::Controls::Label* InnerRadiusText, *InnerRadiusLabel, *OuterRadiusText, *OuterRadiusLabel;
	
	uint8_t* Selected;

};
*/
#endif